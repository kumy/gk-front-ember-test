{{#if user}}
  <span class="user">
    {{#link-to 'user' user.id}}{{user.name}}{{/link-to}}
  </span>
{{else}}
  <span class="user user-anonymous">
    {{#if (has-block)}}
      {{yield}}
    {{else}}
      Anonymous
    {{/if}}
  </span>
{{/if}}
